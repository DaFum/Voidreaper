import { renderShipCore } from "./core-renderer.js";
import { createModuleCoreRendererRegistry } from "./module-core-renderers.js";
import { renderConnector } from "./connector-renderer.js";
import { renderAdaptiveArmor, renderBalanceDecorators } from "./adaptive-armor-renderer.js";
import { renderActivityAnimations } from "./activity-animation-renderer.js";
import { renderDamageOverlay } from "./damage-overlay-renderer.js";
import { resolveAssemblyLod } from "./assembly-lod-policy.js";

export const ASSEMBLY_RENDER_LAYERS=Object.freeze(["lower-structure","ventral-modules","rear-connectors","core-hull","module-cores","adaptive-armor","moving-parts","energy-effects","damage-effects","target-markers"]);
const DEFAULT_PALETTE=Object.freeze({hull:"#0b1825",metal:"#172b3b",structure:"#162633",armor:"#456273",edge:"#a1efff",energy:"#28e5ff",cockpit:"#e5fcff",thruster:"#ffad42",damage:"#ff3d75",fault:"#d94aff",void:"#46165f"});
export function createAssemblyRenderer(){const modules=createModuleCoreRendererRegistry();return{renderPlayerShip(ctx,{geometrySnapshot,position,rotation=0,time=0,telemetryByNodeId={},lodOptions={}}){if(!geometrySnapshot?.coreGeometry)return false;const lod=resolveAssemblyLod({zoom:1,visibleSegments:geometrySnapshot.nodes.length-1,particlePressure:0,...lodOptions}),palette={...DEFAULT_PALETTE,...geometrySnapshot.shipStyle?.palette};ctx.save();ctx.translate(position.x,position.y);ctx.rotate(rotation);for(const connector of geometrySnapshot.connections)renderConnector(ctx,connector,palette,{lod,energyFlow:time});renderBalanceDecorators(ctx,geometrySnapshot.decorators,palette);renderShipCore(ctx,geometrySnapshot.coreGeometry,palette,{time,lod});for(const node of geometrySnapshot.nodes){if(node.isRoot)continue;const activity=telemetryByNodeId[node.nodeId]??{charge:0,cooldown:0,firing:false,heat:0,energyFlow:.35,faulting:false,activeUnits:0};ctx.save();ctx.translate(node.worldPosition.x,node.worldPosition.y);ctx.rotate(node.worldRotation);modules.render(node.profile.rendererId,ctx,{size:node.geometry.size,variantSeed:node.variantSeed,rotation:0,palette,activity,damageState:node.damageState,time,lod});ctx.restore();}renderAdaptiveArmor(ctx,geometrySnapshot.armor,palette);renderActivityAnimations(ctx,geometrySnapshot,{time,lod,palette,telemetryByNodeId});for(const node of geometrySnapshot.nodes)if(!node.isRoot&&node.damageState!=="intact")renderDamageOverlay(ctx,node,{time,palette,lod});ctx.restore();return true;}};}
