import { normalize, perpendicular, scale, sub } from "../../features/ship-assembly/geometry/vector-math.js";
const FAMILY_DETAIL=Object.freeze({"tapered-blade":.52,"heavy-block":.92,"phase-shard":.38,"thermal-open":.3,"void-organic":.64,"carrier-frame":.48,"reaper-curve":.45,streamline:.34,"industrial-truss":.72,"null-fracture":.42});
export function renderAdaptiveArmor(ctx, plates, palette) { ctx.save();ctx.lineJoin="round";for(const plate of plates){const n=perpendicular(normalize(sub(plate.end,plate.start))),w=7*(FAMILY_DETAIL[plate.family]??.5)*plate.taper,a={x:plate.start.x+n.x*w,y:plate.start.y+n.y*w},b={x:plate.end.x+n.x*w*.7,y:plate.end.y+n.y*w*.7},c={x:plate.end.x-n.x*w*.7,y:plate.end.y-n.y*w*.7},d={x:plate.start.x-n.x*w,y:plate.start.y-n.y*w};ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.lineTo(c.x,c.y);ctx.lineTo(d.x,d.y);ctx.closePath();ctx.fillStyle=plate.family==="void-organic"||plate.family==="null-fracture"?palette.void:palette.armor;ctx.strokeStyle=palette.edge;ctx.fill();ctx.stroke();}ctx.restore(); }
export function renderBalanceDecorators(ctx, decorators, palette) { ctx.save();ctx.strokeStyle=palette.armor;ctx.fillStyle=palette.structure;for(const d of decorators){ctx.save();ctx.translate(d.position.x,d.position.y);ctx.rotate(d.rotation);ctx.scale(d.scale,d.scale);ctx.beginPath();if(d.kind==="cooling-ribs"){for(let i=-2;i<=2;i++){ctx.moveTo(i*5,-8);ctx.lineTo(i*7,9);}}else if(d.kind==="counterweight"){ctx.arc(0,0,10,0,Math.PI*2);ctx.moveTo(-16,0);ctx.lineTo(16,0);}else{ctx.moveTo(-16,-6);ctx.lineTo(16,0);ctx.lineTo(-16,6);ctx.closePath();}ctx.fill();ctx.stroke();ctx.restore();}ctx.restore(); }
