export function createDetachedDebris(geometry,{x,y,rotation=0,vx=0,vy=0,spin=.8,lifetime=3}={}){return{geometry,x,y,rotation,vx,vy,spin,lifetime,age:0};}
export function updateDetachedDebris(pool,dt){for(const debris of pool){debris.age+=dt;debris.x+=debris.vx*dt;debris.y+=debris.vy*dt;debris.rotation+=debris.spin*dt;}return pool.filter(debris=>debris.age<debris.lifetime);}
export function renderDetachedDebris(ctx,pool,renderModule){for(const debris of pool){ctx.save();const alpha=Math.max(0,1-debris.age/debris.lifetime);ctx.globalAlpha=alpha;ctx.translate(debris.x,debris.y);ctx.rotate(debris.rotation);ctx.shadowColor="#ff5500";ctx.shadowBlur=15*alpha;renderModule(ctx,debris.geometry,"detached-preview");ctx.restore();}}
