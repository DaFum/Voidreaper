export function renderActivityAnimations(ctx,snapshot,{time,lod,palette,telemetryByNodeId}){ctx.save();ctx.globalCompositeOperation="lighter";for(const node of snapshot.nodes){if(node.isRoot)continue;const activity=telemetryByNodeId[node.nodeId];if(!activity?.firing&&!(activity?.energyFlow>0))continue;const pulse=.45+Math.sin(time*7+node.variantSeed)*.25;ctx.globalAlpha=lod==="low"?.35:pulse;ctx.fillStyle=activity.faulting?palette.fault:palette.energy;ctx.beginPath();ctx.arc(node.worldPosition.x,node.worldPosition.y,2+(activity.charge??0)*4,0,Math.PI*2);ctx.fill();}ctx.restore();}
