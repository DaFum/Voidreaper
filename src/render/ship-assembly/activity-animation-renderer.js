export function renderActivityAnimations(ctx,snapshot,{time,lod,palette,telemetryByNodeId,buildAnimations=[]}){ctx.save();ctx.globalCompositeOperation="lighter";for(const node of snapshot.nodes){if(node.isRoot)continue;const activity=telemetryByNodeId[node.nodeId],build=buildAnimations.find(item=>item.nodeId===node.nodeId);if(!activity?.firing&&!(activity?.energyFlow>0)&&!build)continue;const pulse=.45+Math.sin(time*7+node.variantSeed)*.25;ctx.globalAlpha=lod==="low"?.35:pulse;ctx.fillStyle=activity?.faulting?palette.fault:build?.phase==="power-up"?"#ffc857":palette.energy;ctx.beginPath();ctx.arc(node.worldPosition.x,node.worldPosition.y,2+(activity?.charge??build?.phaseProgress??0)*4,0,Math.PI*2);ctx.fill();}ctx.restore();}
