export function renderAssemblyThumbnail(renderer,snapshot,{size=192,background="#07111c"}={}){const canvas=typeof OffscreenCanvas!=="undefined"?new OffscreenCanvas(size,size):Object.assign(document.createElement("canvas"),{width:size,height:size}),ctx=canvas.getContext("2d"),bounds=snapshot.totalBounds??{minX:-50,minY:-50,maxX:50,maxY:50},scale=Math.min((size-24)/(bounds.maxX-bounds.minX||1),(size-24)/(bounds.maxY-bounds.minY||1));ctx.fillStyle=background;ctx.fillRect(0,0,size,size);ctx.save();ctx.translate(size/2,size/2);ctx.scale(scale,scale);renderer.renderPlayerShip(ctx,{geometrySnapshot:snapshot,position:{x:-(bounds.minX+bounds.maxX)/2,y:-(bounds.minY+bounds.maxY)/2},lodOptions:{userSetting:"low"}});ctx.restore();return canvas;}
