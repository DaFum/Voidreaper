const COSTS = Object.freeze({ mhp: [25,20], mdmg: [30,25], mspd: [25,20], mxp: [35,25], mshard: [40,30], mreroll: [60,50], mbanish: [60,50], mrevive: [200,250], mmag: [25,20], mluck: [45,35] });
export function investedLegacyShards(meta = {}) { return Object.entries(COSTS).reduce((total, [id, [base, step]]) => { const level = Math.max(0, Number(meta[id]) || 0); return total + Array.from({ length: level }, (_, index) => base + index * step).reduce((sum, cost) => sum + cost, 0); }, 0); }
export function convertLegacyMeta(save) { const refund = investedLegacyShards(save.legacy?.meta),alreadyRefunded=save.migrationHistory.some(entry => entry.type === "legacy-meta-refund"); if(refund&&!alreadyRefunded){save.currencies.voidShards += refund;save.unlocks.veteranProfile = true;save.unlocks["badge-eternal-veteran"] = true;save.migrationHistory.push({ type: "legacy-meta-refund", refund, at: new Date().toISOString() });}if(save.legacy)save.legacy.meta={};return save; }
