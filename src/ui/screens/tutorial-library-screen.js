import { escapeHtml } from "../escape-html.js";
export function renderTutorialLibrary(root,{chapters,onStart=()=>{},onResume=()=>{}}={}){root.innerHTML=`<section class="tutorial-library" data-tutorial-id="tutorial-library"><header><span>VR // TRAINING ARCHIVE</span><h2>TUTORIALS</h2><p>Starte Kapitel erneut oder setze eine pausierte Führung fort.</p></header><div class="tutorial-library__grid">${chapters.map(ch=>`<article data-state="${ch.active?"active":ch.completed?"completed":ch.available?"available":"locked"}" data-tutorial-id="tutorial-chapter-${escapeHtml(ch.id)}"><small>${ch.completed?"ABGESCHLOSSEN":ch.active?"AKTIV":ch.available?"BEREIT":"NOCH NICHT ENTDECKT"}</small><h3>${escapeHtml(ch.title)}</h3><p>${escapeHtml(ch.description)}</p>${ch.available?`<button class="btn small" data-chapter-id="${escapeHtml(ch.id)}" data-mode="${ch.completed?"replay":"guided"}">${ch.active?"FORTSETZEN":ch.completed?"WIEDERHOLEN":"STARTEN"}</button>`:`<em>${escapeHtml(ch.lockedReason)}</em>`}</article>`).join("")}</div></section>`;root.onclick=e=>{const button=e.target.closest("[data-chapter-id]");if(!button)return;const chapter=chapters.find(item=>item.id===button.dataset.chapterId);if(chapter?.active)onResume();else onStart(button.dataset.chapterId,button.dataset.mode);};}
