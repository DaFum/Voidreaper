import { createBlueprintCard } from "./blueprint-card.js";
export function renderBlueprintLibrary(root,{blueprints,onOpen,onFavorite,onImport,onCreate}={}){root.innerHTML=`<section class="blueprint-library"><header><div><small>ARCHITEKTUR-ARCHIV</small><h2>SCHIFFSBAUPLÄNE</h2></div><div><button type="button" data-action="import">IMPORT</button><button type="button" data-action="create">AKTUELLE KONSTRUKTION SPEICHERN</button></div></header><div class="blueprint-library__grid"></div></section>`;const grid=root.querySelector(".blueprint-library__grid");for(const blueprint of blueprints)grid.append(createBlueprintCard(blueprint,{onOpen,onFavorite}));if(!blueprints.length)grid.innerHTML=`<div class="blueprint-library__empty"><strong>KEINE SIGNALSTRUKTUREN</strong><span>Speichere eine Konstruktion oder importiere einen Code.</span></div>`;root.querySelector('[data-action="import"]').onclick=()=>onImport?.();root.querySelector('[data-action="create"]').onclick=()=>onCreate?.();}
