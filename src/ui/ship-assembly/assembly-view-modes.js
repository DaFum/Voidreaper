export const ASSEMBLY_VIEW_MODES=Object.freeze({NORMAL:"normal",STRUCTURE:"structure",ENERGY:"energy",DAMAGE:"damage",FLIGHT:"flight"});
export const ASSEMBLY_VIEW_MODE_LABELS=Object.freeze({normal:"KONSTRUKTION",structure:"STRUKTUR",energy:"ENERGIE",damage:"SCHADEN",flight:"FLUGPROFIL"});
export function renderAssemblyToolbar(root,active,onChange){root.innerHTML=Object.values(ASSEMBLY_VIEW_MODES).map(mode=>`<button data-mode="${mode}" aria-pressed="${mode===active}">${ASSEMBLY_VIEW_MODE_LABELS[mode]}</button>`).join("");root.onclick=event=>{const mode=event.target.closest("[data-mode]")?.dataset.mode;if(mode)onChange(mode);};}
export function getViewModeOverlay(mode,{assembly,geometry,flightProfile}={}){if(mode===ASSEMBLY_VIEW_MODES.STRUCTURE)return{ports:Object.values(assembly.portsById),connections:[...Object.values(assembly.connectionsById),...Object.values(assembly.secondaryConnectionsById)],labels:geometry.nodes.map(n=>({position:n.worldPosition,text:`T${assembly.portsById[n.parentPortId]?.branchDepth??0}`}))};if(mode===ASSEMBLY_VIEW_MODES.ENERGY)return{connections:geometry.connections.map(c=>({...c,label:c.cable.energyClass})),labels:[]};if(mode===ASSEMBLY_VIEW_MODES.DAMAGE)return{labels:geometry.nodes.map(n=>({position:n.worldPosition,text:n.isRoot?"KERN":`${Math.round(n.armorIntegrity??0)}A / ${Math.round(n.coreIntegrity??0)}C`}))};if(mode===ASSEMBLY_VIEW_MODES.FLIGHT)return{centerOfMass:flightProfile?.centerOfMass,thrustVectors:flightProfile?.thrustVectors??[],labels:[{position:{x:0,y:0},text:`MASSE ${flightProfile?.totalMass??"—"}`} ]};return{};}
