export function renderAssemblyInspector(root,{node,port,item,deltas=[],warnings=[]}={}){const title=node?.definitionId??item?.definitionId??port?.key??"KEINE AUSWAHL";root.innerHTML=`<header><small>ASSEMBLY // INSPEKTION</small><h2>${title}</h2></header><dl>${deltas.map(([key,value])=>`<div><dt>${key}</dt><dd>${value}</dd></div>`).join("")}</dl>${warnings.length?`<ul class="assembly-warnings">${warnings.map(text=>`<li>${text}</li>`).join("")}</ul>`:""}<div class="assembly-inspector__actions"><button data-action="rotate">DREHEN</button><button data-action="move-branch">AST VERSETZEN</button><button data-action="dismantle">DEMONTIEREN</button></div>`;return root;}
