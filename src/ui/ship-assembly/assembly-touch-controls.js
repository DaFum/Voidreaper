export function attachAssemblyTouchControls(element,{onPrevious,onNext,onSelect,onDetails}={}){let start=null,longPress=null;const clear=()=>{clearTimeout(longPress);longPress=null;};const down=event=>{const point=event.touches?.[0]??event;start={x:point.clientX,y:point.clientY,time:performance.now()};longPress=setTimeout(()=>onDetails?.(),520);};const up=event=>{if(!start)return;const point=event.changedTouches?.[0]??event,dx=point.clientX-start.x,dy=point.clientY-start.y,duration=performance.now()-start.time;clear();if(Math.abs(dx)>48&&Math.abs(dx)>Math.abs(dy))dx<0?onNext?.():onPrevious?.();else if(duration<500)onSelect?.(event);start=null;};element.addEventListener("touchstart",down,{passive:true});element.addEventListener("touchend",up,{passive:true});element.addEventListener("touchcancel",clear,{passive:true});return()=>{clear();element.removeEventListener("touchstart",down);element.removeEventListener("touchend",up);element.removeEventListener("touchcancel",clear);};}
