const ENERGY_PATTERN=Object.freeze({standard:1,precision:2,thermal:3,void:4,heavy:5});
function tracePort(ctx,port,r){ctx.beginPath();if(port.sizeClass==="S"){for(let i=0;i<6;i++){const a=i/6*Math.PI*2;i?ctx.lineTo(Math.cos(a)*r,Math.sin(a)*r):ctx.moveTo(Math.cos(a)*r,Math.sin(a)*r);}ctx.closePath();}else if(port.sizeClass==="M"){ctx.arc(0,0,r,0,Math.PI*2);ctx.moveTo(r*.62,0);ctx.arc(0,0,r*.62,0,Math.PI*2);}else if(port.sizeClass==="L"){ctx.moveTo(-r,-r*.8);ctx.lineTo(-r,r*.8);ctx.lineTo(-r*.55,r);ctx.moveTo(r,-r*.8);ctx.lineTo(r,r*.8);ctx.lineTo(r*.55,r);}else{ctx.rect(-r,-r*.72,r*2,r*1.44);ctx.moveTo(-r*.55,-r);ctx.lineTo(-r*.55,r);ctx.moveTo(r*.55,-r);ctx.lineTo(r*.55,r);}}
export function renderPortOverlay(ctx,ports,{selectedPortId,compatibilityByPortId={}}={}){ctx.save();for(const port of ports){const position=port.worldPosition??port.localPosition??{x:0,y:0},result=compatibilityByPortId[port.portId],compatible=result?.compatible!==false;ctx.save();ctx.translate(position.x,position.y);ctx.rotate(Math.atan2(port.direction?.y??0,port.direction?.x??1));ctx.globalAlpha=compatible?1:.27;ctx.strokeStyle=port.portId===selectedPortId?"#ffc857":"#48eaff";ctx.lineWidth=Math.max(1.5,(port.loadCapacity??4)/4);tracePort(ctx,port,6+({S:0,M:2,L:4,XL:7}[port.sizeClass]??0));ctx.stroke();ctx.lineWidth=1;const pattern=ENERGY_PATTERN[port.energyClass]??1;for(let i=0;i<pattern;i++){ctx.beginPath();ctx.moveTo(-4+i*2,-3);ctx.lineTo(-4+i*2,3);ctx.stroke();}ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(10,0);ctx.stroke();ctx.restore();}ctx.restore();}
export function portAccessibilityLabel(port,result){const state=port.occupiedByNodeId?"belegt":result?.compatible===false?`inkompatibel: ${result.reasonLabels?.join(", ")}`:"verfügbar";return`${port.sizeClass}-Port, ${port.mountType}, Energie ${port.energyClass}, Traglast ${port.loadCapacity}, ${state}`;}
