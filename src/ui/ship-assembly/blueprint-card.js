import { escapeHtml, safeImageDataUrl } from "../escape-html.js";
export function createBlueprintCard(blueprint,{onOpen,onFavorite}={}){const card=document.createElement("article"),thumbnail=safeImageDataUrl(blueprint.thumbnailDataUrl);card.className="blueprint-card";card.innerHTML=`<button type="button" class="blueprint-card__open" aria-label="${escapeHtml(blueprint.name)} öffnen"><span class="blueprint-card__frame">${thumbnail?`<img src="${thumbnail}" alt="">`:`<i>NO // THUMB</i>`}</span><span class="blueprint-card__meta"><small>${escapeHtml(blueprint.shipFrameId)} // ${Number(blueprint.nodes?.length??0)} KNOTEN</small><strong>${escapeHtml(blueprint.name)}</strong><span>ABYSS ${Number(blueprint.usage?.highestAbyssDepth??0)} · ${escapeHtml(blueprint.usage?.lastUsedAt?new Date(blueprint.usage.lastUsedAt).toLocaleDateString():"UNBENUTZT")}</span></span></button><button type="button" class="blueprint-card__favorite" aria-label="Favorit" aria-pressed="${Boolean(blueprint.favorite)}">◆</button>`;card.querySelector(".blueprint-card__open").onclick=()=>onOpen?.(blueprint.blueprintId);card.querySelector(".blueprint-card__favorite").onclick=()=>onFavorite?.(blueprint.blueprintId,!blueprint.favorite);return card;}
