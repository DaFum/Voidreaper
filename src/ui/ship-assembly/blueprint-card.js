export function createBlueprintCard(blueprint,{onOpen,onFavorite}={}){const card=document.createElement("article");card.className="blueprint-card";card.innerHTML=`<button class="blueprint-card__open" aria-label="${blueprint.name} öffnen"><span class="blueprint-card__frame">${blueprint.thumbnailDataUrl?`<img src="${blueprint.thumbnailDataUrl}" alt="">`:`<i>NO // THUMB</i>`}</span><span class="blueprint-card__meta"><small>${blueprint.shipFrameId} // ${blueprint.nodes.length} KNOTEN</small><strong>${blueprint.name}</strong><span>ABYSS ${blueprint.usage?.highestAbyssDepth??0} · ${blueprint.usage?.lastUsedAt?new Date(blueprint.usage.lastUsedAt).toLocaleDateString():"UNBENUTZT"}</span></span></button><button class="blueprint-card__favorite" aria-label="Favorit" aria-pressed="${blueprint.favorite}">◆</button>`;card.querySelector(".blueprint-card__open").onclick=()=>onOpen?.(blueprint.blueprintId);card.querySelector(".blueprint-card__favorite").onclick=()=>onFavorite?.(blueprint.blueprintId,!blueprint.favorite);return card;}
