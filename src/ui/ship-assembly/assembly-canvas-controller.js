export function createAssemblyCanvasController({canvas,camera={rotation:0,zoom:1,offset:{x:0,y:0}}}){let pointer=null;const api={camera,rotateBy(delta){camera.rotation+=delta;return camera;},zoomBy(delta){camera.zoom=Math.max(.55,Math.min(1.8,camera.zoom+delta));return camera;},panBy(dx,dy){camera.offset.x+=dx;camera.offset.y+=dy;return camera;},resetView(){camera.rotation=0;camera.zoom=1;camera.offset={x:0,y:0};return camera;},screenToWorld(x,y){const rect=canvas.getBoundingClientRect();return{x:(x-rect.left-rect.width/2)/camera.zoom-camera.offset.x,y:(y-rect.top-rect.height/2)/camera.zoom-camera.offset.y};}};canvas?.addEventListener("wheel",event=>{event.preventDefault();api.zoomBy(-Math.sign(event.deltaY)*.08);},{passive:false});canvas?.addEventListener("pointerdown",event=>{pointer={x:event.clientX,y:event.clientY};canvas.setPointerCapture(event.pointerId);});canvas?.addEventListener("pointermove",event=>{if(!pointer)return;api.panBy((event.clientX-pointer.x)/camera.zoom,(event.clientY-pointer.y)/camera.zoom);pointer={x:event.clientX,y:event.clientY};});canvas?.addEventListener("pointerup",()=>pointer=null);return api;}
