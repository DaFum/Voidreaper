const STYLE=Object.freeze({exact:{stroke:"#48eaff",dash:[]},compatible:{stroke:"#ffc857",dash:[8,3]},structural:{stroke:"#a6b7c4",dash:[3,5]},incompatible:{stroke:"#ff4d79",dash:[2,6]},blocked:{stroke:"#a52c55",dash:[1,8]}});
export function renderBlueprintGhostOverlay(ctx,ghostNodes){ctx.save();for(const ghost of ghostNodes){const style=STYLE[ghost.match]??STYLE.structural;ctx.save();ctx.translate(ghost.position.x,ghost.position.y);ctx.rotate(ghost.rotation??0);ctx.globalAlpha=ghost.match==="exact"?.22:.48;ctx.strokeStyle=style.stroke;ctx.setLineDash(style.dash);ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(0,-ghost.size);ctx.lineTo(ghost.size*.75,ghost.size*.7);ctx.lineTo(-ghost.size*.75,ghost.size*.7);ctx.closePath();ctx.stroke();ctx.restore();}ctx.restore();}
