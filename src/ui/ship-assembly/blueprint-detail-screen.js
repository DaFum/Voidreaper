import { escapeHtml, safeImageDataUrl } from "../escape-html.js";
export function renderBlueprintDetail(root,{blueprint,onAction}={}){const thumbnail=safeImageDataUrl(blueprint.thumbnailDataUrl);root.innerHTML=`<section class="blueprint-detail"><header><small>${escapeHtml(blueprint.shipFrameId)} // ${Number(blueprint.nodes?.length??0)} KNOTEN</small><h2>${escapeHtml(blueprint.name)}</h2></header><div class="blueprint-detail__preview">${thumbnail?`<img src="${thumbnail}" alt="Bauplan ${escapeHtml(blueprint.name)}">`:"KEINE MINIATUR"}</div><div class="blueprint-detail__actions">${[["activate","STANDARD"],["rename","UMBENENNEN"],["duplicate","DUPLIZIEREN"],["variant","VARIANTE"],["export","EXPORT"],["delete","LÖSCHEN"]].map(([action,label])=>`<button data-action="${action}">${label}</button>`).join("")}</div></section>`;root.onclick=event=>{const action=event.target.closest("[data-action]")?.dataset.action;if(action)onAction?.(action,blueprint);};}
