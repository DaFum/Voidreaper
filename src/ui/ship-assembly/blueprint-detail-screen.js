export function renderBlueprintDetail(root,{blueprint,onAction}={}){root.innerHTML=`<section class="blueprint-detail"><header><small>${blueprint.shipFrameId} // ${blueprint.nodes.length} KNOTEN</small><h2>${blueprint.name}</h2></header><div class="blueprint-detail__preview">${blueprint.thumbnailDataUrl?`<img src="${blueprint.thumbnailDataUrl}" alt="Bauplan ${blueprint.name}">`:"KEINE MINIATUR"}</div><div class="blueprint-detail__actions">${[["activate","STANDARD"],["rename","UMBENENNEN"],["duplicate","DUPLIZIEREN"],["variant","VARIANTE"],["export","EXPORT"],["delete","LÖSCHEN"]].map(([action,label])=>`<button data-action="${action}">${label}</button>`).join("")}</div></section>`;root.onclick=event=>{const action=event.target.closest("[data-action]")?.dataset.action;if(action)onAction?.(action,blueprint);};}
