import { decodeBlueprint } from "../../features/ship-assembly/blueprints/blueprint-codec.js";
export function createBlueprintImportDialog(root,{validate,onImport,onCancel}={}){root.innerHTML=`<dialog class="blueprint-import"><form method="dialog"><header><small>EXTERNE KONSTRUKTION</small><h2>BAUPLAN IMPORTIEREN</h2></header><label>BASE64URL-CODE<textarea data-role="code" rows="7" spellcheck="false"></textarea></label><div data-role="report" aria-live="polite"></div><footer><button value="cancel">ABBRECHEN</button><button data-action="inspect" type="button">PRÜFEN</button><button data-action="import" type="button" disabled>IMPORTIEREN</button></footer></form></dialog>`;const dialog=root.querySelector("dialog"),report=root.querySelector('[data-role="report"]'),importButton=root.querySelector('[data-action="import"]');let result=null;root.querySelector('[data-action="inspect"]').onclick=()=>{try{result=validate(decodeBlueprint(root.querySelector("textarea").value));const status=document.createElement("strong"),issues=document.createElement("ul");status.textContent=result.valid?"VALIDIERT":"NICHT VERWENDBAR";for(const issue of result.issues){const item=document.createElement("li");item.textContent=`${issue.type}: ${issue.definitionId??issue.nodeId??issue.connectionId??""}`;issues.append(item);}report.replaceChildren(status,issues);importButton.disabled=!result.valid;}catch(error){result=null;report.textContent=error.message;importButton.disabled=true;}};importButton.onclick=()=>{onImport?.(result.blueprint);dialog.close();};dialog.addEventListener("close",()=>{if(dialog.returnValue==="cancel")onCancel?.();});return{open:()=>dialog.showModal(),close:()=>dialog.close()};}
