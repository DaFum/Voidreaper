export function createEquipmentService({registry,inventory}){const damageModifiers=new Map();return{requireInstance(id){const item=inventory().find(entry=>entry.instanceId===id);if(!item)throw new Error(`Unknown equipment instance: ${id}`);return item;},requireAssemblyProfile(id){return registry.requireAssemblyProfile(id);},setDamageModifiers(instanceId,modifiers){damageModifiers.set(instanceId,structuredClone(modifiers));},getModifiers(instanceId){return structuredClone(damageModifiers.get(instanceId)??[]);},isEnabled(instanceId){return !this.getModifiers(instanceId).some(modifier=>modifier.stat==="enabled"&&modifier.value===false);}};}
