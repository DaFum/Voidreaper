import { MODULE_VISUAL_PROFILE_BY_ID } from "./module-visual-profiles.js";
const tagsOf = definition => new Set((definition.tags ?? []).map(tag => typeof tag === "string" ? tag : tag.id));
function selectProfile(definition) { const tags = tagsOf(definition); if (definition.slot === "reactor") return "reactor-aux"; if (definition.unique) return tags.has("Corruption") || tags.has("Void") ? "corrupted-node" : "structure-spine"; if (tags.has("Drone")) return "drone-bay"; if (tags.has("Beam")) return "weapon-beam"; if (tags.has("Mine")) return "weapon-mine"; if (tags.has("Shield")) return "shield-emitter"; if (tags.has("Cooling") || tags.has("Heat")) return "cooling-array"; if (tags.has("Orbit")) return "orbit-hub"; if (tags.has("Void") || tags.has("Anomaly")) return "void-anomaly"; if (tags.has("Explosive") || tags.has("Homing")) return "weapon-missile"; if (tags.has("Projectile") || tags.has("Critical") || definition.slot === "weapon") return "weapon-linear"; if (tags.has("Structure") || tags.has("Adapter")) return "structure-spine"; if (tags.has("Sensor") || tags.has("Targeting")) return "sensor-array"; return "utility-node"; }
export function resolveModuleAssemblyProfile(definition) { if (definition.assembly) return structuredClone(definition.assembly); const visual = MODULE_VISUAL_PROFILE_BY_ID.get(selectProfile(definition)); const sizeClass = definition.assemblySizeClass ?? visual.sizeClass; const mass = definition.assemblyMass ?? ({ S:3,M:6,L:11,XL:18 }[sizeClass]); return { visualProfileId: visual.id, rendererId: visual.rendererId, variantSeed: definition.id, sizeClass, mountTypes: definition.assemblyMountTypes ?? visual.preferredMounts, loadDemand: definition.assemblyLoadDemand ?? Math.ceil(mass * .8), energyClass: definition.assemblyEnergyClass ?? (tagsOf(definition).has("Void") ? "void" : tagsOf(definition).has("Heat") ? "thermal" : "standard"), mass, damage: definition.assemblyDamage ?? { armor: 28 + mass * 2, core: 18 + mass }, childPorts: definition.assemblyChildPorts ?? (visual.id === "structure-spine" ? [{ key:"branch",sizeClass:"M",mountType:"lateral",loadCapacity:8,energyClass:"standard",acceptedEnergyClasses:["standard","precision","thermal","void"] }] : []), requiresWorkbench: definition.corruption > 0 || ["L","XL"].includes(sizeClass) || visual.id === "structure-spine", hitShape: visual.id.includes("orbit") || visual.id.includes("shield") ? "ring" : visual.id.includes("weapon") ? "capsule" : "circle" };
}
