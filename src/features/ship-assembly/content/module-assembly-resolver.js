import { MODULE_VISUAL_PROFILE_BY_ID } from "./module-visual-profiles.js";
import { hashString } from "../../../core/rng.js";
const tagsOf = definition => new Set((definition.tags ?? []).map(tag => typeof tag === "string" ? tag : tag.id));
function selectProfile(definition) { const tags = tagsOf(definition); if (definition.slot === "reactor") return "reactor-aux"; if (definition.unique) return tags.has("Corruption") || tags.has("Void") ? "corrupted-node" : "structure-spine"; if (tags.has("Drone")) return "drone-bay"; if (tags.has("Beam")) return "weapon-beam"; if (tags.has("Mine")) return "weapon-mine"; if (tags.has("Shield")) return "shield-emitter"; if (tags.has("Cooling") || tags.has("Heat")) return "cooling-array"; if (tags.has("Orbit")) return "orbit-hub"; if (tags.has("Void") || tags.has("Anomaly")) return "void-anomaly"; if (tags.has("Explosive") || tags.has("Homing")) return "weapon-missile"; if (tags.has("Projectile") || tags.has("Critical") || definition.slot === "weapon") return "weapon-linear"; if (tags.has("Structure") || tags.has("Adapter")) return "structure-spine"; if (tags.has("Sensor") || tags.has("Targeting")) return "sensor-array"; return "utility-node"; }
function defaultChildPorts(visual,sizeClass){const make=(key,size,mount,x,y,load)=>({key,sizeClass:size,mountType:mount,direction:{x,y},localPosition:{x:x*34,y:y*34},loadCapacity:load,energyClass:"standard",acceptedEnergyClasses:["standard","precision","heavy","thermal","void"]});if(visual.id==="structure-spine")return[make("branch-forward","L","structural",0,1,14),make("branch-port","M","lateral",-1,.2,8),make("branch-starboard","M","lateral",1,.2,8),make("branch-dorsal","S","dorsal",0,-1,4)];if(["L","XL"].includes(sizeClass))return[make("utility-branch","M","lateral",1,.25,8)];if(sizeClass==="M")return[make("micro-port","S","dorsal",0,-1,4)];return[];}
const requiresWorkbench=(definition,profile)=>Boolean(profile.requiresWorkbench||definition.corruption>0||["L","XL"].includes(profile.sizeClass)||profile.visualProfileId==="structure-spine"||profile.visualProfileId==="corrupted-node");
export function resolveModuleAssemblyProfile(definition) { if (definition.assembly){const profile=structuredClone(definition.assembly);if (profile.variantSeed === undefined) profile.variantSeed = hashString(definition.id);profile.requiresWorkbench=requiresWorkbench(definition,profile);return profile;} const visualProfileId=selectProfile(definition),visual = MODULE_VISUAL_PROFILE_BY_ID.get(visualProfileId);if(!visual)throw new Error(`Unknown module visual profile "${visualProfileId}" for definition "${definition.id}"`); const sizeClass = definition.assemblySizeClass ?? visual.sizeClass; const mass = definition.assemblyMass ?? ({ S:3,M:6,L:11,XL:18 }[sizeClass]); const profile={ visualProfileId: visual.id, rendererId: visual.rendererId, variantSeed: hashString(definition.id), sizeClass, mountTypes: definition.assemblyMountTypes ?? visual.preferredMounts, loadDemand: definition.assemblyLoadDemand ?? Math.ceil(mass * .8), energyClass: definition.assemblyEnergyClass ?? (tagsOf(definition).has("Void") ? "void" : tagsOf(definition).has("Heat") ? "thermal" : "standard"), mass, damage: definition.assemblyDamage ?? { armor: 28 + mass * 2, core: 18 + mass }, childPorts: definition.assemblyChildPorts ?? defaultChildPorts(visual,sizeClass), requiresWorkbench:false, hitShape: visual.id.includes("orbit") || visual.id.includes("shield") ? "ring" : visual.id.includes("weapon") ? "capsule" : "circle" };profile.requiresWorkbench=requiresWorkbench(definition,profile);return profile;
}
