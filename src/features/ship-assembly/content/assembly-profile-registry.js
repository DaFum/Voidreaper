export function createAssemblyProfileRegistry() { const shipFrames = new Map(); const moduleProfiles = new Map(); const portProfiles = new Map(); return { registerShipFrame(profile) { shipFrames.set(profile.id, Object.freeze(structuredClone(profile))); }, registerModuleProfile(profile) { moduleProfiles.set(profile.id, Object.freeze(structuredClone(profile))); }, registerPortProfile(profile) { portProfiles.set(profile.id, Object.freeze(structuredClone(profile))); }, getShipFrame: id => shipFrames.get(id) ?? null, getModuleProfile: id => moduleProfiles.get(id) ?? null, getPortProfile: id => portProfiles.get(id) ?? null, getCounts: () => ({ shipFrames: shipFrames.size, moduleProfiles: moduleProfiles.size, portProfiles: portProfiles.size }) }; }
