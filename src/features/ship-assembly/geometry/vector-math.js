export const add=(a,b)=>({x:a.x+b.x,y:a.y+b.y}); export const sub=(a,b)=>({x:a.x-b.x,y:a.y-b.y}); export const scale=(v,n)=>({x:v.x*n,y:v.y*n}); export const length=v=>Math.hypot(v.x,v.y); export const normalize=v=>{const n=length(v)||1;return{x:v.x/n,y:v.y/n}}; export const perpendicular=v=>({x:-v.y,y:v.x}); export const rotate=(v,a)=>({x:v.x*Math.cos(a)-v.y*Math.sin(a),y:v.x*Math.sin(a)+v.y*Math.cos(a)}); export const interpolate=(a,b,t)=>({x:a.x+(b.x-a.x)*t,y:a.y+(b.y-a.y)*t});
