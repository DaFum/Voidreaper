export function createGeometryCache(){return{revision:-1,shipFrameId:null,coreGeometry:null,shipStyle:null,nodeGeometry:new Map(),connectionGeometry:new Map(),armorGeometry:new Map(),decorators:[],occupiedBounds:[],totalBounds:null};}
export function calculateTotalBounds(bounds){if(!bounds.length)return{minX:-48,minY:-48,maxX:48,maxY:48};return bounds.reduce((total,item)=>({minX:Math.min(total.minX,item.minX),minY:Math.min(total.minY,item.minY),maxX:Math.max(total.maxX,item.maxX),maxY:Math.max(total.maxY,item.maxY)}),{minX:Infinity,minY:Infinity,maxX:-Infinity,maxY:-Infinity});}
