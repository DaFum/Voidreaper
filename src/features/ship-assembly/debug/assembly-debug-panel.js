import { escapeHtml } from "../../../ui/escape-html.js";
export function createAssemblyDebugPanel(root,service){root.innerHTML=`<aside class="assembly-debug"><header><small>DEV // ASSEMBLY</small><button data-action="close">×</button></header><label>SZENARIO<select data-role="scenario">${service.listScenarios().map(id=>`<option value="${escapeHtml(id)}">${escapeHtml(id)}</option>`).join("")}</select></label><button data-action="run">SZENARIO AUSFÜHREN</button><label>NODE<select data-role="node"></select></label><div class="assembly-debug__grid"><button data-action="armor">ARMOR BROKEN</button><button data-action="core">CORE DISRUPTED</button><button data-action="detach">DETACH</button><button data-action="branch">DETACH BRANCH</button></div><fieldset><label><input type="checkbox" data-flag="assemblyHitZones"> HIT ZONES</label><label><input type="checkbox" data-flag="assemblyPorts"> PORTS</label><label><input type="checkbox" data-flag="assemblyMass"> MASS</label></fieldset><pre data-role="output"></pre></aside>`;const output=root.querySelector('[data-role="output"]'),nodes=root.querySelector('[data-role="node"]'),refresh=()=>{const snapshot=service.getSnapshot();nodes.innerHTML=Object.values(snapshot?.nodesById??{}).map(node=>`<option value="${escapeHtml(node.nodeId)}">${escapeHtml(node.definitionId)} // ${escapeHtml(node.nodeId)}</option>`).join("");};root.onclick=async event=>{const action=event.target.closest("[data-action]")?.dataset.action,nodeId=nodes.value;try{let result;if(action==="run")result=await service.runScenario(root.querySelector('[data-role="scenario"]').value);if(action==="armor")result=service.setDamageState(nodeId,"armor-broken");if(action==="core")result=service.setDamageState(nodeId,"core-disrupted");if(action==="detach")result=service.detachNode(nodeId);if(action==="branch")result=service.detachBranch(nodeId);if(action==="close")root.hidden=true;output.textContent=JSON.stringify(result,null,2);refresh();}catch(error){output.textContent=error.stack??error.message;}};root.onchange=event=>{const flag=event.target.dataset.flag;if(flag){if(flag==="assemblyHitZones")service.showHitZones(event.target.checked);if(flag==="assemblyPorts")service.showPorts(event.target.checked);if(flag==="assemblyMass")service.showMass(event.target.checked);}};refresh();return{refresh,show:()=>{root.hidden=false;refresh();},hide:()=>root.hidden=true};}
