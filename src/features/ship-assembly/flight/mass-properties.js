export const clamp=(value,min,max)=>Math.max(min,Math.min(max,value));
export function calculateMassProperties(nodes,rootPosition={x:0,y:0}){const totalMass=nodes.reduce((sum,node)=>sum+(node.mass??node.visualMass??0),0),centerOfMass=nodes.reduce((sum,node)=>({x:sum.x+node.worldPosition.x*(node.mass??node.visualMass??0),y:sum.y+node.worldPosition.y*(node.mass??node.visualMass??0)}),{x:0,y:0});centerOfMass.x/=totalMass||1;centerOfMass.y/=totalMass||1;const rotationalInertia=nodes.reduce((sum,node)=>{const dx=node.worldPosition.x-centerOfMass.x,dy=node.worldPosition.y-centerOfMass.y;return sum+(node.mass??node.visualMass??0)*(dx*dx+dy*dy);},0);return{totalMass,centerOfMass,rotationalInertia,lateralImbalance:centerOfMass.x-rootPosition.x};}
export function mapToFlightModifiers(properties,thrust={}){return{accelerationMultiplier:clamp(1-properties.totalMass/260,.75,1.08),turnMultiplier:clamp(1-properties.rotationalInertia/900000,.85,1.08),dodgeDistanceMultiplier:clamp(1+(thrust.dodgeAuthority??0)/100-properties.totalMass/520,.8,1.15),dodgeCooldownMultiplier:clamp(1+properties.totalMass/650-(thrust.lateral??0)/130,.85,1.25),driftBias:clamp(properties.lateralImbalance/900,-.04,.04),recoilControl:clamp((thrust.structuralSupport??0)/100,0,.35)};}
