import { clamp } from "./mass-properties.js";
function calculateAssemblyZoom(bounds,viewport){const width=bounds.width??bounds.maxX-bounds.minX,height=bounds.height??bounds.maxY-bounds.minY,widthRatio=viewport.width/Math.max(width*2.7,viewport.width),heightRatio=viewport.height/Math.max(height*2.7,viewport.height);return clamp(Math.min(widthRatio,heightRatio),.82,1);}
export function createCameraFitService({camera,viewport}){let target=1;return{fit(bounds){target=calculateAssemblyZoom(bounds,viewport());return target;},update(dt,corePosition){const factor=1-Math.exp(-dt/.8);camera.zoom=(camera.zoom??1)+(target-(camera.zoom??1))*factor;camera.x=corePosition.x;camera.y=corePosition.y;return camera;},getTarget:()=>target};}
