export function createAssemblyRevisionTracker(){const revisions={structure:0,visual:0,damage:0,activity:0};return{mark(kind){if(!(kind in revisions))throw new Error(`Unknown assembly revision kind: ${kind}`);revisions[kind]+=1;return revisions[kind];},sync(snapshot){revisions.structure=Math.max(revisions.structure,snapshot.structuralRevision??0);revisions.visual=Math.max(revisions.visual,snapshot.visualRevision??0);},snapshot:()=>Object.freeze({...revisions}),changedSince(previous,kind){return revisions[kind]!==previous[kind];}};}
