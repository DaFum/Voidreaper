const ASSEMBLY_BUDGET=Object.freeze({maxVisibleSegments:18,maxVisibleDecorators:6,maxBranchDepth:4,maxSecondaryConnections:10,maxDamageParticlesPerModule:12,maxDetachedDebris:8,maxGeometryRebuildsPerFrame:1,maxThumbnailSize:320});
export function enforceAssemblyBudget(snapshot){const nodes=snapshot.nodes.slice(0,ASSEMBLY_BUDGET.maxVisibleSegments+1),retainedNodeIds=new Set(nodes.map(node=>node.nodeId)),connections=snapshot.connections.filter(item=>retainedNodeIds.has(item.sourceNodeId)&&retainedNodeIds.has(item.targetNodeId)),retainedConnectionIds=new Set(connections.map(item=>item.id));return{...snapshot,nodes,connections,armor:snapshot.armor.filter(item=>retainedConnectionIds.has(item.connectionId)),decorators:snapshot.decorators.slice(0,ASSEMBLY_BUDGET.maxVisibleDecorators)};}
