export function createPendingMount({itemInstance,source,acquiredAt,requiresWorkbench}){return{pendingMountId:`pending-${itemInstance.instanceId}`,itemInstanceId:itemInstance.instanceId,definitionId:itemInstance.definitionId,source,acquiredAt,requiresWorkbench,suggestionPortIds:[],status:"awaiting-placement"};}
export function createPendingMountService({runInventory}){return{queue({itemInstance,profile,source="loot",acquiredAt=0}){const requiresWorkbench=Boolean(profile.requiresWorkbench||["L","XL"].includes(profile.sizeClass)||profile.visualProfileId==="structure-spine"||profile.visualProfileId==="corrupted-node");const pending=createPendingMount({itemInstance,source,acquiredAt,requiresWorkbench});runInventory.addPending(pending);return pending;},setSuggestions(id,ids){return runInventory.updatePending(id,{suggestionPortIds:[...ids]});},complete(id,nodeId){return runInventory.updatePending(id,{status:"mounted",mountedNodeId:nodeId});},defer(id){return runInventory.updatePending(id,{status:"stored"});},cancel(id){return runInventory.updatePending(id,{status:"stored"});},values:()=>runInventory.pending()};}
