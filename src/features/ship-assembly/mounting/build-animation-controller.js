const BUILD_PHASES=Object.freeze([{id:"port-glow",duration:.12},{id:"extend-braces",duration:.18},{id:"lock-core",duration:.16},{id:"connect-lines",duration:.14},{id:"close-armor",duration:.22},{id:"power-up",duration:.18}]);
export function createBuildAnimationController({reducedMotion=false}={}){const animations=[];let total=0;for(let i=0;i<BUILD_PHASES.length;i++)total+=BUILD_PHASES[i].duration;return{start(nodeId,{mode="mount",workbench=false}={}){const duration=(workbench?1.6:.8)*(reducedMotion?.45:1);const animation={nodeId,mode,elapsed:0,duration,complete:false};animations.push(animation);return Object.freeze({...animation});},update(dt){let writeIndex=0;for(let i=0;i<animations.length;i++){const animation=animations[i];animation.elapsed=Math.min(animation.duration,animation.elapsed+dt);animation.complete=animation.elapsed>=animation.duration;if(!animation.complete)animations[writeIndex++]=animation;}animations.length=writeIndex;},snapshot(){const snap=new Array(animations.length);const lastPhase=BUILD_PHASES[BUILD_PHASES.length-1];for(let i=0;i<animations.length;i++){const animation=animations[i];const normalized=animation.elapsed/animation.duration;let cursor=0,phase=lastPhase,phaseProgress=1;for(let j=0;j<BUILD_PHASES.length;j++){const candidate=BUILD_PHASES[j];const share=candidate.duration/total;if(normalized<=cursor+share){phase=candidate;phaseProgress=(normalized-cursor)/share;break;}cursor+=share;}snap[i]=Object.freeze({...animation,phase:phase.id,phaseProgress:Math.max(0,Math.min(1,phaseProgress))});}return snap;}};}
