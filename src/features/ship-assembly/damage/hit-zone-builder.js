export function createHitZone({id,ownerType,ownerId,shape,transform,priority}){return Object.freeze({id,ownerType,ownerId,shape,transform,priority,enabled:true});}
export function buildAssemblyHitZones(geometrySnapshot,shipProfile,{onFallback=console.warn}={}){const zones=[createHitZone({id:`zone-core-${geometrySnapshot.shipFrameId}`,ownerType:"core",ownerId:geometrySnapshot.nodes.find(node=>node.isRoot)?.nodeId,shape:shipProfile.coreHitZone,transform:{position:{x:0,y:0},rotation:0},priority:1})];for(const node of geometrySnapshot.nodes){if(node.isRoot)continue;const kind=node.profile.hitShape??node.geometry.hitShape?.kind;let shape;if(kind==="ring")shape={kind:"ring",innerRadius:node.geometry.size*.38,outerRadius:node.geometry.size*.72};else if(kind==="capsule")shape={kind:"capsule",length:node.geometry.length,radius:node.geometry.size*.5};else if(kind==="polygon")shape={kind:"polygon",points:[{x:-node.geometry.size,y:-node.geometry.size*.6},{x:node.geometry.size,y:-node.geometry.size*.6},{x:node.geometry.size*.8,y:node.geometry.size*.6},{x:-node.geometry.size*.8,y:node.geometry.size*.6}]};else{if(!kind)onFallback?.(`[assembly] fallback hit circle: ${node.definitionId}`);shape={kind:"circle",radius:node.geometry.size*.62};}zones.push(createHitZone({id:`zone-module-${node.nodeId}`,ownerType:"module",ownerId:node.nodeId,shape,transform:{position:node.worldPosition,rotation:node.worldRotation},priority:10}));}return zones;}
