export function findSurvivingSecondaryConnection(snapshot,childNodeId){return Object.values(snapshot.secondaryConnectionsById).find(connection=>{const touches=connection.sourceNodeId===childNodeId||connection.targetNodeId===childNodeId,other=connection.sourceNodeId===childNodeId?connection.targetNodeId:connection.sourceNodeId;return touches&&Boolean(snapshot.nodesById[other]);})??null;}
export function createBranchFailureService({assemblyService,geometryService}){return{resolveNodeLoss(nodeId){const snapshot=assemblyService.getSnapshot(),children=Object.values(snapshot.nodesById).filter(node=>node.parentNodeId===nodeId);for(const child of children){const bridge=findSurvivingSecondaryConnection(snapshot,child.nodeId);if(bridge){assemblyService.promoteSecondaryConnection(child.nodeId,bridge.connectionId);assemblyService.applyBranchPenalty(child.nodeId,"secondary-support");continue;}if(geometryService?.canCreateEmergencyBrace?.(child.nodeId)){assemblyService.createEmergencyBrace(child.nodeId);assemblyService.applyBranchPenalty(child.nodeId,"emergency-brace");continue;}assemblyService.detachNode({nodeId:child.nodeId,detachBranch:true});}assemblyService.detachNode({nodeId,detachBranch:false});}};}
