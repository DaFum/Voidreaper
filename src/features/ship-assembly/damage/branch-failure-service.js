export function findSurvivingSecondaryConnection(snapshot,childNodeId){return Object.values(snapshot.secondaryConnectionsById).find(connection=>{const touches=connection.sourceNodeId===childNodeId||connection.targetNodeId===childNodeId,other=connection.sourceNodeId===childNodeId?connection.targetNodeId:connection.sourceNodeId;return touches&&Boolean(snapshot.nodesById[other]);})??null;}
export function createBranchFailureService({assemblyService,geometryService}){return{resolveNodeLoss(nodeId){const initial=assemblyService.getSnapshot(),childIds=Object.values(initial.nodesById).filter(node=>node.parentNodeId===nodeId).map(node=>node.nodeId);for(const childNodeId of childIds){const live=assemblyService.getSnapshot();if(!live.nodesById[childNodeId])continue;const bridge=findSurvivingSecondaryConnection(live,childNodeId);if(bridge){const otherNodeId=bridge.sourceNodeId===childNodeId?bridge.targetNodeId:bridge.sourceNodeId;if(live.nodesById[otherNodeId]){assemblyService.promoteSecondaryConnection(childNodeId,bridge.connectionId);assemblyService.applyBranchPenalty(childNodeId,"secondary-support");continue;}}if(geometryService?.canCreateEmergencyBrace?.(childNodeId)){assemblyService.createEmergencyBrace(childNodeId);assemblyService.applyBranchPenalty(childNodeId,"emergency-brace");continue;}assemblyService.detachNode({nodeId:childNodeId,detachBranch:true});}assemblyService.detachNode({nodeId,detachBranch:false});}};}
