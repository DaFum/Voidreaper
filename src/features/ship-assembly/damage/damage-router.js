export function sortHitCandidates(candidates){return[...candidates].sort((a,b)=>a.distanceAlongRay-b.distanceAlongRay||b.zone.priority-a.zone.priority);}
export function createDamageRouter({moduleDamageService,playerDamageService,shieldService}){return{route({hit,damage,penetration=0}){if(shieldService?.absorb?.(hit,damage))return{absorbed:true,remainingDamage:0};let remainingDamage=damage,remainingPenetration=penetration;for(const candidate of sortHitCandidates(hit.candidates)){if(candidate.ignored)continue;if(candidate.zone.ownerType==="module"){const result=moduleDamageService.applyDamage(candidate.zone.ownerId,remainingDamage,hit.damageType);remainingDamage=result.remainingDamage;remainingPenetration-=result.penetrationCost;}else{playerDamageService.applyHullDamage(remainingDamage,hit.damageType);remainingDamage=0;}if(remainingDamage<=0||remainingPenetration<0)break;}return{remainingDamage,remainingPenetration};},routeArea({zones,totalDamage,cap=totalDamage*.45,damageType}){const amount=Math.min(cap,totalDamage/Math.max(1,zones.length));return zones.map(zone=>zone.ownerType==="module"?moduleDamageService.applyDamage(zone.ownerId,amount,damageType):playerDamageService.applyHullDamage(amount,damageType));}};}
