import { DAMAGE_BEHAVIORS } from "../../../content/ship-assembly/module-damage-behaviors.js";
export function getDamageModifiers(node,visualProfileId){if(node.damageState==="intact")return[];if(node.damageState==="armor-broken")return[{stat:"stability",operation:"add",value:-.15,source:"assembly-damage"}];if(node.damageState==="core-disrupted")return(DAMAGE_BEHAVIORS[visualProfileId]??DAMAGE_BEHAVIORS.default).disruptedModifiers.map(modifier=>({...modifier,source:"assembly-damage"}));return[{stat:"enabled",operation:"override",value:false,source:"assembly-damage"}];}
export function createModuleFaultAdapter({assemblyService,equipmentService,eventBus}){let attachedInstanceIds=new Set();const refresh=()=>{const snapshot=assemblyService.getSnapshot(),currentInstanceIds=new Set(Object.values(snapshot.nodesById).map(node=>node.moduleInstanceId).filter(Boolean));for(const instanceId of attachedInstanceIds)if(!currentInstanceIds.has(instanceId))equipmentService.setDamageModifiers(instanceId,[]);for(const node of Object.values(snapshot.nodesById))if(node.moduleInstanceId)equipmentService.setDamageModifiers(node.moduleInstanceId,getDamageModifiers(node,node.visualProfileId));attachedInstanceIds=currentInstanceIds;};const off=eventBus?.on("assembly:changed",refresh);return{refresh,destroy:()=>off?.()};}
