export function resolveDamageState(node){if(node.damageState==="detached"||node.coreIntegrity<=0)return"detached";if(node.coreIntegrity<node.maxCoreIntegrity*.45)return"core-disrupted";if(node.armorIntegrity<=0)return"armor-broken";return"intact";}
export function createModuleDamageService({assemblyService,eventBus,branchFailureService}){return{applyDamage(nodeId,amount,damageType){const node=assemblyService.requireNode(nodeId);let remaining=amount,armorAbsorb=Math.min(node.armorIntegrity,remaining);node.armorIntegrity-=armorAbsorb;remaining-=armorAbsorb;if(remaining>0)node.coreIntegrity-=remaining;const previousState=node.damageState;node.damageState=resolveDamageState(node);assemblyService.publishDamageChange(nodeId);if(node.damageState!==previousState)eventBus?.emit("assembly:damage-state-changed",{nodeId,previousState,nextState:node.damageState,damageType});if(node.damageState==="detached")branchFailureService?.resolveNodeLoss(nodeId);return{remainingDamage:Math.max(0,-node.coreIntegrity),penetrationCost:armorAbsorb>0?1:0};}};}
