import { renderAssemblyThumbnail } from "../../../render/ship-assembly/assembly-thumbnail-renderer.js";
async function canvasDataUrl(canvas){if(canvas.toDataURL)return canvas.toDataURL("image/webp",.82);const blob=await canvas.convertToBlob({type:"image/webp",quality:.82});return await new Promise(resolve=>{const reader=new FileReader();reader.onload=()=>resolve(reader.result);reader.readAsDataURL(blob);});}
export function createBlueprintThumbnailService({assemblyRenderer,geometryService,size=240}){const cache=new Map();return{async render(_assemblySnapshot){const geometry=geometryService.getSnapshot(),key=`${geometry.shipFrameId}:${geometry.revision}:${size}`;if(cache.has(key))return cache.get(key);const data=await canvasDataUrl(renderAssemblyThumbnail(assemblyRenderer,geometry,{size}));cache.set(key,data);return data;},clear:()=>cache.clear()};}
