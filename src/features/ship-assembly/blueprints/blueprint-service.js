import { createBlueprint,toBlueprintNode } from "./blueprint-schema.js";
export function createBlueprintService({saveStore,idFactory,thumbnailService}){let cached={},activeId=null;const requireBlueprint=id=>{const blueprint=cached[id];if(!blueprint)throw new Error(`Unknown ship blueprint: ${id}`);return blueprint;},persist=async mutator=>{const next=await saveStore.update(save=>{save.shipBlueprints??={};mutator(save);return save;});cached=structuredClone(next.shipBlueprints??{});activeId=next.activeBlueprintId??null;return next;};return{hydrate(meta){cached=structuredClone(meta.shipBlueprints??{});activeId=meta.activeBlueprintId??null;},async saveFromAssembly({name,assemblySnapshot,replaceBlueprintId=null,veteran=false}){const id=replaceBlueprintId??idFactory.create("ship-blueprint"),previous=cached[id],blueprint=createBlueprint({id,name:name.trim().slice(0,48)||"Unbenannter Bauplan",shipFrameId:assemblySnapshot.shipFrameId,nodes:Object.values(assemblySnapshot.nodesById).map(toBlueprintNode),connections:Object.values(assemblySnapshot.secondaryConnectionsById),variants:previous?.visualVariants??[]});if(previous)blueprint.createdAt=previous.createdAt;if(veteran)blueprint.veteranDamage=Object.values(assemblySnapshot.nodesById).filter(node=>node.damageState!=="intact").map(node=>({blueprintNodeId:node.nodeId,damageState:node.damageState}));blueprint.thumbnailDataUrl=await thumbnailService?.render?.(assemblySnapshot)??null;await persist(save=>save.shipBlueprints[id]=blueprint);return structuredClone(blueprint);},async duplicate(id){const source=requireBlueprint(id),copyId=idFactory.create("ship-blueprint"),copy=structuredClone(source);copy.blueprintId=copyId;copy.name=`${source.name} Kopie`;copy.createdAt=copy.updatedAt=new Date().toISOString();await persist(save=>save.shipBlueprints[copyId]=copy);return structuredClone(copy);},async rename(id,name){const clean=name.trim().slice(0,48);if(!clean)throw new Error("Blueprint name must not be empty");await persist(save=>{const item=save.shipBlueprints[id];if(!item)throw new Error(`Unknown ship blueprint: ${id}`);item.name=clean;item.updatedAt=new Date().toISOString();});},async delete(id){await persist(save=>{delete save.shipBlueprints[id];if(save.activeBlueprintId===id)save.activeBlueprintId=null;});},async setActive(id){requireBlueprint(id);await persist(save=>save.activeBlueprintId=id);},async createVariant(id,name,snapshot){const variant={variantId:idFactory.create("blueprint-variant"),name:name.trim().slice(0,48)||"Variante",nodes:Object.values(snapshot.nodesById).map(toBlueprintNode),connections:Object.values(snapshot.secondaryConnectionsById),createdAt:new Date().toISOString()};await persist(save=>save.shipBlueprints[id].visualVariants.push(variant));return structuredClone(variant);},list:()=>Object.values(cached).map(structuredClone).sort((a,b)=>Number(b.favorite)-Number(a.favorite)||b.updatedAt.localeCompare(a.updatedAt)),require:id=>structuredClone(requireBlueprint(id)),getActiveId:()=>activeId};}
