export function encodeBlueprint(blueprint){const json=JSON.stringify({v:blueprint.blueprintVersion,f:blueprint.shipFrameId,n:blueprint.nodes,c:blueprint.connections,x:blueprint.visualVariants}),bytes=new TextEncoder().encode(json);let binary="";for(const byte of bytes)binary+=String.fromCharCode(byte);return btoa(binary).replaceAll("+","-").replaceAll("/","_").replaceAll("=","");}
export function decodeBlueprint(code){const clean=String(code).trim();if(!/^[A-Za-z0-9_-]+$/.test(clean))throw new Error("Ungültiges Bauplanformat");const normalized=clean.replaceAll("-","+").replaceAll("_","/"),binary=atob(normalized.padEnd(Math.ceil(normalized.length/4)*4,"=")),bytes=Uint8Array.from(binary,char=>char.charCodeAt(0)),data=JSON.parse(new TextDecoder().decode(bytes));return{blueprintVersion:data.v,shipFrameId:data.f,nodes:data.n??[],connections:data.c??[],visualVariants:data.x??[]};}
