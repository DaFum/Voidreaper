{
  "name": "voidreaper-eternal-redux",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --test",
    "build": "npm run validate-content && npm run validate:assembly && vite build",
    "preview": "vite preview",
    "validate-content": "node scripts/validate-content.mjs",
    "validate:assembly": "node scripts/validate-ship-assembly-content.mjs"
  },
  "devDependencies": {
    "vite": "^8.1.4",
    "vitest": "^4.1.10"
  }
}
